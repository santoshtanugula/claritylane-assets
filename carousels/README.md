# carousels

Rendered LinkedIn document carousels (PDF + cover PNG), produced by
`scripts/render_carousel.py` in claritylane-socialmedia. Buffer fetches them from here by
public URL — there is no upload mutation.
